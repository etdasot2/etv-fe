/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['picsum.photos', 'eternal-all.s3.ap-southeast-1.amazonaws.com', 'reelmint-all.s3.eu-north-1.amazonaws.com'],
      },
};

export default nextConfig;
